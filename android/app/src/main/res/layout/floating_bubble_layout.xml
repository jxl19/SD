<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="65dp"
        android:id="@+id/floating_bubble_root"
        android:layout_height="wrap_content"
        android:orientation="vertical">

    <!--<ImageButton-->
        <!--android:id="@+id/floating_head_image_button"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:src="@drawable/button_transparent"-->

    <!--/>-->
    <Button 
        android:text="Button"
        android:id="@+id/btnDoMagic"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="20dp"
        />
    <!--Profile image for the chat head.-->
    <ImageView
            android:id="@+id/floating_head_image"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="20dp"
            tools:ignore="ContentDescription"/>

    <!--Close button-->
    <!-- <ImageView
            android:id="@+id/close_button"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_marginLeft="40dp"
            android:src="@drawable/cancel"
            tools:ignore="ContentDescription"/> -->
</RelativeLayout>